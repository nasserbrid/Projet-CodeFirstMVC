@model IEnumerable<CodeFirstMVC.Models.Salarie>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sal_nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sal_age)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sal_description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Sal_nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sal_age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sal_description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Sal_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Sal_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Sal_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
